Control,Pass / Fail,Finding (what is wrong or missing),Remediation (exact change needed)
1. Least privilege,Fail,"The aws_iam_role_policy (receipts_processor_policy) assigns full S3 admin rights (Action: [""s3:*""]) across all AWS resources (Resource: [""*""]).","Restrict Action to only required S3 API calls ([""s3:GetObject"", ""s3:PutObject""]) and scope Resource explicitly to the payment receipts bucket ARN: Resource: [""${aws_s3_bucket.payment_receipts.arn}/*""]."
2. Encryption at rest and in transit,Fail,"The aws_s3_bucket (payment_receipts) lacks server-side encryption configuration, allowing receipts to be stored in plain text.","Add an aws_s3_bucket_server_side_encryption_configuration resource specifying sse_algorithm = ""aws:kms"" or sse_algorithm = ""AES256"" within apply_server_side_encryption_by_default."
3. No hardcoded secrets,Fail,"DB_PASSWORD = ""kijani-prod-password-2024"" is hardcoded in plain text in the aws_lambda_function environment block.","Change DB_PASSWORD = ""kijani-prod-password-2024"" to DB_PASSWORD = var.db_password. Declare variable ""db_password"" { type = string; sensitive = true } in variables.tf, and source the secret dynamically at runtime via AWS Secrets Manager (aws_secretsmanager_secret_version)."
4. Naming and tagging,Fail,"Bucket name uses generic my-payment-receipts-bucket and no resource tags (Environment, Owner, CostCenter) exist across any defined infrastructure blocks.","Standardize bucket name to kk-receipts-prod-af-south-1 and attach a mandatory tags block to all resources: tags = { Environment = ""production"", Service = ""kk-receipts"", ManagedBy = ""terraform"" }."
5. Auditability and logging,Fail,"The S3 bucket lacks access logging and CloudTrail object-level logging configurations, leaving data access unmonitored.","Define an aws_s3_bucket_logging resource directing access logs to an existing audit bucket (target_bucket = aws_s3_bucket.audit_logs.id, target_prefix = ""s3-access-logs/receipts/"")."
6. Data classification and residency,Fail,"Provider sets region = ""us-east-1"", violating data sovereignty rules for East African financial and customer payment data.","Change provider region in provider ""aws"" block to an approved African region: region = ""af-south-1"" (Cape Town) to keep payment data local."
