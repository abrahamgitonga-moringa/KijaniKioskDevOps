apiVersion: v1
kind: Service
metadata:
  name: kk-payments-service
  namespace: default
  labels:
    app: kk-payments
spec:
  type: NodePort
  selector:
    app: kk-payments
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
      protocol: TCP
