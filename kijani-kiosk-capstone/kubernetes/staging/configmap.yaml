apiVersion: v1
kind: ConfigMap
metadata:
  name: kk-payments-config
  namespace: kijani-staging
  labels:
    app: kk-payments
    environment: kijani-staging
data:
  DB_HOST: "staging-db.internal"
  RECEIPT_BUCKET: "kk-payments-receipts-staging"
  LOG_LEVEL: "DEBUG"
