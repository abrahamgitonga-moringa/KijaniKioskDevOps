{
  "name": "kijanikiosk-payments",
  "version": "1.0.0",
  "description": "KijaniKiosk Payments Service",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building payments service...' && mkdir -p dist && echo 'console.log(\"Payments Service v1.0\");' > dist/app.js",
    "test": "echo 'ci-pipeline-verification: deliberate failure' && node -e 'process.exit(1)'"
  }
}
